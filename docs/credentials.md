# Admin Credentials

Default admin credentials seeded into the `users` table:

- Username: `Rowena Coronado`
- Password: `IamRowena2026`

These credentials are used by the login page through the database-backed authentication flow.
